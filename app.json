{
  "name": "Instagram Creator Bot",
  "description": "Telegram bot for Instagram account creation with Selenium automation",
  "env": {
    "BOT_TOKEN": {
      "description": "Telegram Bot Token from @BotFather",
      "required": true
    },
    "DATABASE_URL": {
      "description": "PostgreSQL connection URL (auto-set by Heroku Postgres addon)",
      "required": true
    },
    "STATIC_PASSWORD": {
      "description": "Password for all created Instagram accounts",
      "required": false,
      "value": "SecurePassword123!"
    },
    "HEADLESS_MODE": {
      "description": "Run browser in headless mode",
      "required": false,
      "value": "True"
    },
    "DELAY_BETWEEN_ACCOUNTS": {
      "description": "Delay in seconds between creating accounts",
      "required": false,
      "value": "30"
    },
    "LOG_LEVEL": {
      "description": "Logging level (DEBUG, INFO, WARNING, ERROR)",
      "required": false,
      "value": "INFO"
    },
    "ADMIN_IDS": {
      "description": "Comma-separated Telegram admin user IDs",
      "required": false
    },
    "SPREADSHEET_NAME": {
      "description": "Google Sheets spreadsheet name for saving accounts",
      "required": false
    },
    "CREDENTIALS_FILE": {
      "description": "Google service account credentials JSON file path",
      "required": false
    }
  },
  "addons": [
    "heroku-postgresql:hobby-dev"
  ],
  "buildpacks": [
    {
      "url": "https://github.com/heroku/heroku-buildpack-google-chrome"
    },
    {
      "url": "https://github.com/heroku/heroku-buildpack-python"
    }
  ],
  "formation": {
    "web": {
      "quantity": 1,
      "size": "basic"
    }
  }
}
